# IDE
.idea

# Docker (Setup Ghost)
project/config.production.json
# Docker (Testing suite local execution)
project/screenshots
project/reports
project/videos

# Monkey (Cypress)
# Ver .gitignore embebido

# Ripper (Playwright)
project/modules/ripper-playwright/.png\n*
project/modules/ripper-playwright/screenshots*/
project/modules/ripper-playwright/temp*/
project/modules/ripper-playwright/*graph.json
project/modules/ripper-playwright/*graph2.json
project/modules/ripper-playwright/*graph3.json
project/modules/ripper-playwright/firebaseConfig.js
project/modules/ripper-playwright/results/

# E2E (Cypress)
project/modules/e2e-cypress/cypress/screenshots
project/modules/e2e-cypress/cypress/reports
project/modules/e2e-cypress/cypress/videos

# E2E (Playwright)
project/modules/e2e-playwright/test-results/
project/modules/e2e-playwright/playwright-report/
project/modules/e2e-playwright/blob-report/
project/modules/e2e-playwright/playwright/.cache/
project/modules/e2e-playwright/screenshots/

# E2E (Puppeteer)
project/modules/e2e-puppeteer/screenshots

# E2E (Kraken)
project/modules/e2e-kraken/reports

# Helper (Nodemailer)
node_modules
.env

# ===== Terraform =====
**/secure/
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# ===== Vagrant =====
.vagrant/
